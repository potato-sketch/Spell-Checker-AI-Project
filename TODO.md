# TODO

## Filipino/Tagalog suggestion accuracy fixes (module-safe)

- [x] Inspect backend logic and identify candidate filtering / phonetic / language-hint issues in `project.py`.
- [x] Implement Steps A–D in `project.py` (backend-only; keep modules intact).

- [ ] Verify with manual test inputs in the running server (Taglish misspellings + hyphen/affix cases).
- [ ] (Optional) Add a small test harness for repeatable checks.

